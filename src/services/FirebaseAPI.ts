import * as firebase from 'firebase/app';
import "firebase/analytics";
import "firebase/firestore";

import NewLetter from '../components/NewLetter';


class FirebaseAPI {

  static init() {
    const firebaseConfig = {

    }
  }

  static addNewLetter({ name, message, location }: NewLetter) {

  }
}